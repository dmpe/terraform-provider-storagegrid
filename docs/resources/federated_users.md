---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "storagegrid_federated_users Resource - storagegrid"
subcategory: ""
description: |-
  Import a federated user into the StorageGrid.
  Use this resource to automate the import of a federated user into the StorageGrid to create dependent resources such as
  access keys etc.
  Workflow
  If the federated user does not yet exist in StorageGRID, the user will be imported to the StorageGRID when creating this resource.If the user does already exist in the StorageGRID, the available user data will be read to this resource during creation.
  [!warning] Only available in StorageGRID 12 or higher
  This resource is only available when working with StorageGRID 12 or higher.
  Targeting earlier StorageGRID versions will result in errors during the Terraform apply step.
---

# storagegrid_federated_users (Resource)

Import a federated user into the StorageGrid.

Use this resource to automate the import of a federated user into the StorageGrid to create dependent resources such as
access keys etc.

**Workflow**
- If the federated user does not yet exist in StorageGRID, the user will be imported to the StorageGRID when creating this resource.
- If the user does already exist in the StorageGRID, the available user data will be read to this resource during creation.

> [!warning] Only available in StorageGRID 12 or higher
> This resource is only available when working with StorageGRID 12 or higher.
> Targeting earlier StorageGRID versions will result in errors during the Terraform apply step.

## Example Usage

```terraform
resource "storagegrid_federated_users" "test" {
  unique_name = "federated-user/federated-user-unique-name"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `unique_name` (String) The unique name of the user to import - must match the name of the user in the identity provider.

The unique name must be prefixed with 'federated-user', such that the resulting unique name is of the form 'federated-user/<name>'.

### Read-Only

- `account_id` (String)
- `full_name` (String) The human-readable name for the User
- `id` (String) The ID of this resource.
- `member_of` (List of String) Group memberships for this User
- `user_urn` (String)
